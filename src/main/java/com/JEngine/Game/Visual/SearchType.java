package com.JEngine.Game.Visual;

/**
 * Search type enum describes the type of search you would like to do when searching a JScene
 */
public enum SearchType {
    SearchByTag,
    SearchByName,
    SearchByNameAndTag
}
